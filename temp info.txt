main steps:
	1.create a form to collect data.
	2.add button to submit(preven default).
	3.add a table to show the data entered.
	4.add to each row the ability to delete and edit;
		4a.on delete --> delete the whole row.
		4b.on edit --> keep the row index, and update(no add!).